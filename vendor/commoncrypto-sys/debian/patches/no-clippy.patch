Index: commoncrypto-sys/Cargo.toml
===================================================================
--- commoncrypto-sys.orig/Cargo.toml
+++ commoncrypto-sys/Cargo.toml
@@ -8,13 +8,8 @@ repository = "https://github.com/malept/
 keywords = ["crypto", "hash", "digest", "osx", "commoncrypto"]
 license = "MIT"
 
-[features]
-lint = ["clippy"]
-
 [dependencies]
 libc = "0.2"
 
-clippy = { version = "0.0", optional = true }
-
 [dev-dependencies]
 hex = "0.2"
